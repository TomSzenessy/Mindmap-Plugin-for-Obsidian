{
  "name": "tomindmap",
  "version": "0.9.0",
  "private": true,
  "description": "Keyboard-first mind mapping for Obsidian Canvas",
  "scripts": {
    "build": "node scripts/inline-runtime-modules.js",
    "build:check": "node scripts/inline-runtime-modules.js --check",
    "test": "node --test tests/*.test.js",
    "check": "npm run build:check && npm test && node --check main.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
